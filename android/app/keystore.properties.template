# Signing key configuration template
# Copy this to keystore.properties and fill in your values
# DO NOT commit keystore.properties to git

storeFile=../keystore/wishpool-release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=wishpool-key
keyPassword=YOUR_KEY_PASSWORD