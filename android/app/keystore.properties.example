# Keystore configuration for Android release builds
# Copy this file to keystore.properties and replace with your actual values
# NEVER commit keystore.properties to git - it contains sensitive passwords

storeFile=../keystore/wishpool-release.jks
storePassword=YOUR_STORE_PASSWORD_HERE
keyAlias=wishpool-key
keyPassword=YOUR_KEY_PASSWORD_HERE